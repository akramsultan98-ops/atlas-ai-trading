"""ATLAS V2 — a strategy factory for Binance spot with a hard AI containment boundary.

The advisory plane (research, generation, analysis) never holds exchange credentials and
never writes execution state. The control plane (risk, execution, monitoring, kill
switch) is deterministic code only. See docs/SPECIFICATION.md section 2.
"""

__version__ = "0.1.0"
